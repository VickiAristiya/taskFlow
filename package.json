{
  "name": "taskflow-tests",
  "version": "1.0.0",
  "scripts": {
    "test:api": "newman run test/postman/TaskFlow_API_Collection.json -e test/postman/TaskFlow_Local_Environment.json",
    "test:api:report": "newman run test/postman/TaskFlow_API_Collection.json -e test/postman/TaskFlow_Local_Environment.json -r htmlextra --reporter-htmlextra-export ./reports/api-test-report.html"
  },
  "devDependencies": {
    "newman": "^6.2.2",
    "newman-reporter-htmlextra": "^1.23.0"
  }
}
